.venv/
__pycache__/
*.py[cod]
.env
.env.*
!.env.example
.pytest_cache/
.ruff_cache/
seed/fixtures/*.json
# generated by grafana/ml/build.py; has your stack's hostname resolved into it.
# grafana/provision.py builds the jobs in-process and never reads this file.
grafana/ml/jobs.json
# Legacy location for the write-back audit log. It now lives under
# $XDG_STATE_HOME/turnaround/ -- this stays so an older checkout stays clean.
agent/_writeback.jsonl
*.log
.DS_Store

# secrets
.secrets/
gcp-sa*.json
.claude/launch.json
